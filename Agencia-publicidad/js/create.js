document.addEventListener("DOMContentLoaded", () => {
    const form =document.getElementById("register").value;

    form.addEventListener("submit", (e) => {
        e.preventDefault();
        let valido = true;
    })
    let titulo = form.getElementById("titulo");

    


});